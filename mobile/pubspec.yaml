name: buspawa_mobile
description: BusPawa Transport Management - Mobile Apps

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_dotenv: ^5.1.0
  provider: ^6.1.1
  qr_code_scanner: ^1.0.1
  flutter_local_notifications: ^17.0.0
  geolocator: ^11.0.0
  connectivity_plus: ^6.0.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
